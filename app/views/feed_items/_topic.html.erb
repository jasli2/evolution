<%
feed_item ||= @feed_item
topic = feed_item.item
%>

<i class="activity-icon iconfa-comment"></i>
<%= link_to topic.user.name, topic.user %> 创建了新的问题 <%= link_to topic.title, topic %>.
<span class='activity-time'><%= distance_of_time_in_words_to_now(topic.created_at) + "前" %></span>
